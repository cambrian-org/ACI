# @package _global_

defaults:
  - example
  - override /task: detection

env:
  agents:
    agent_predator:
      eyes:
        eye:
          resolution: [20, 20]
          num_eyes: [2, 1]
          lon_range: [-30, 30]
    agent_prey:
      eyes:
        eye:
          resolution: [20, 20]
          num_eyes: [2, 1]
          lon_range: [-30, 30]
